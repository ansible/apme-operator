apiVersion: apme.ansible.com/v1alpha1
kind: Apme
metadata:
  labels:
    app.kubernetes.io/name: apme-operator
    app.kubernetes.io/managed-by: kustomize
  name: apme
spec:
  version: "2026.8.10"
  replicas: 1
  components:
    gitleaks: true
    collectionHealth: true
    depAudit: true
    ui: true
  exposure:
    route:
      enabled: true
      # Optional: set a custom host (needs routes/custom-host RBAC).
      # Omit host to use the OpenShift-assigned default hostname.
      # host: apme.apps.example.com
  abbenay:
    enabled: false
